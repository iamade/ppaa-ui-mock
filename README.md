# ppaa-ui-mock → PPAA harness UI shell (Phase 1)

PP-138 takeover workspace (Tobi + Akande, per Ade's 2026-09-21 directive).
Spec lives in [issue #1](https://github.com/iamade/ppaa-ui-mock/issues/1).

- `harness` branch: **phase-1 UI shell** — rail (persona roster) + chat thread + approvals drawer, mock state only, three toggleable personas. Dark-first, single accent, no emoji, phone-readable.
- `main` + tag `rejected-ui-2026-09-21`: the agency-intake UI Ade rejected, preserved verbatim as reference (`rejected-mock.html` on this branch).
- GH Pages serves the `harness` branch root — every increment click-through-able for Ade's walk-through.
- Out of scope in P0: voice, animation, real model calls, functional approvals.
